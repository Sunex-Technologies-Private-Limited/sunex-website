# About visual-edit findings

- The annotated shared `Reveal` wrapper contains only non-interactive opacity and decorative solar-layer treatment; it does not control the leadership image crop and should remain unchanged for this fix.
- The person-image issue is localized to the About leadership cards (`.team-card > img`) and requires card-specific crop and focal-point refinement rather than a global reveal change.
- The top About image panel also uses a broad innovation-team image; preserve its existing editorial composition unless the leadership crop review identifies a related shared sizing rule.
- The leadership-card follow-up replaces direct image sizing with a common portrait-media frame and applies calibrated focal points and tonal adjustments per leader. This keeps all heads fully inside equal-height image areas and makes Umesh’s previously underlit portrait visibly clearer without changing the supplied source images.
- The latest failed visual-editor target is the About introduction group at `About.tsx:18`, not the leadership section. Desktop and mobile review point to a readability refinement: stabilize the large headline into deliberate lines, prevent any copy overflow, and give the image caption a more opaque readable surface.
- Follow-up desktop and mobile checks confirm that the introduction headline, explanatory copy, and image caption now remain inside their intended layout areas with no visible truncation or text/background contrast loss.
- The failed Product target at `Product.tsx:35` is the UrbanTree system image panel. Its over-image status caption needs the same protected readable treatment as the About caption: stable placement, stronger opaque contrast, and no clipping on narrow screens.
- Post-fix Product checks confirm the system-status caption remains fully inside the UrbanTree image panel with an opaque high-contrast background on desktop and mobile; the product visual and surrounding content remain unchanged.
- The final caption-containment correction explicitly anchors the About and Product caption cards within their own panels and places them above reveal decoration. Desktop and 390px mobile captures confirm that the left edge, labels, and all caption lines remain visible.
- The remaining About overflow came from the shared Reveal child rule assigning `position: relative` to the caption. Restoring `position: absolute` on the caption class fixes the root cause. The 536px layout matching the user screenshot and the 1440px desktop layout now keep the full label and caption inside the image panel.
- The City Vision panel now uses the supplied solar UrbanTree biofilter asset (`urbantree-device-solar-biofilter_08fc3e0d.png`) rather than the prior device scene. Portrait-specific focal framing keeps the solar crown and biological core visible in both the desktop split panel and the mobile stacked layout.
- The Product system panel’s blank lower space was caused by its minimum-height image container while the image remained in normal flow. The scene now uses an explicit aspect ratio with the image absolutely filling the panel, so the System status card overlays the photo as intended at both 824px and 390px widths.
- Footer verification: the shared footer now renders the supplied full SunEx Technologies lockup rather than the orange symbol alone. Desktop and 434px mobile reviews confirm the company name remains visible beside the mark.
