# Brand Asset Notes

## Supplied SunEx text lockup

The supplied wide lockup contains the white `SUNEX` wordmark with an orange orbital accent, an orange uppercase `TECHNOLOGIES` line, and the tagline `Powering the Next Clean Future` in white between two short orange rules on a deep navy field. The navigation treatment should preserve this supplied lockup as a compact, readable brand asset beside the orange symbol.

## X sun-sweep reference

The supplied reference shows a prominent orange, gently curved sweep beginning at the lower-left of the X, passing through its center, and rising toward the upper-right. It is a broad solar/trajectory accent rather than a straight tick. The two white diagonal strokes of the X remain visually readable beneath the sweep.

## Faithful navigation lockup comparison

The supplied actual logo uses a compact orange radial-sun symbol at left and a crisp deep-navy `SUNEX` wordmark on light surfaces. In the dark full-lockup reference, `SUNE` is a heavy geometric white wordmark; the X is made of two separate white diagonals, overlaid by a broad orange sweep that starts immediately before the X’s lower-left and rises well beyond its upper-right. `TECHNOLOGIES` is a widely tracked orange uppercase line below, followed by a white `Powering the Next Clean Future` tagline with short orange horizontal rules. The current navigation treatment should use the supplied compact symbol and matching deep-navy/orange text hierarchy, while the custom X should be rebuilt from the two white/foreground diagonal strokes and the prominent orange sweep—not a full orange X diagonal.
